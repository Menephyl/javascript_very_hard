function multi(firstt, seccond) {
    return firstt * seccond
}

function sum(first, second) {
    return first + second
}
const person = {
    name: "Irineu"
}
module.exports = { multi, sum, person }